#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>
#include "SIM_router_power.h"
#include "SIM_router_area.h"

int TestInit(){
}


int main(int, char **){

	//SIM_router_init(&GLOB(router_info), NULL, &GLOB(router_area));

  	//SIM_router_area(&GLOB(router_area));

  	return 0;
}
